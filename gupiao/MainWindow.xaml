<Window x:Class="gupiao.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:gupiao"
        mc:Ignorable="d"
        Title="MainWindow" Height="74" Width="180"  WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResize" Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown" Icon="/股票.png">
    <Border Background="White" CornerRadius="15" BorderThickness="1.5" BorderBrush="Black">
        <Border.ContextMenu>
            <ContextMenu>
                <MenuItem Header="修改代码" Click="MenuItem_Click" />
                <MenuItem Header="退出" Click="MenuItem1_Click"/>
            </ContextMenu>
        </Border.ContextMenu>
        <Grid>
            <StackPanel >
                <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <TextBlock Text="002882"  FontSize="16"   x:Name="tdm"/>
                    <TextBlock Text="金龙羽" FontFamily="red" Margin="10,0,0,0" FontSize="18"  FontWeight="Bold" x:Name="tname"/>
                    <TextBlock/>
                </StackPanel>
                <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,4,0,0">
                    <TextBlock Text="现价："  FontSize="13"  FontWeight="Bold" />
                    <TextBlock Text="1000" Foreground="red"  FontSize="13"  FontWeight="Bold" x:Name="txj"/>
                    <TextBlock Text="涨幅：" FontWeight="Bold"  FontSize="13" Margin="10,0,0,0"/>
                    <TextBlock Text="1000" Foreground="red" FontSize="13"  FontWeight="Bold" x:Name="tzz"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal"  VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,5,0,0">
                    <TextBlock Text="成本："   FontSize="10"  FontWeight="Bold"  />
                    <TextBlock Text="1000" Foreground="red"  FontSize="10"  FontWeight="Bold"  x:Name="tcb"/>
                    <TextBlock Text="盈亏："    FontSize="10"  FontWeight="Bold" Margin="13,0,0,0"/>
                    <TextBlock Text="1000" Foreground="red"  FontSize="10"  FontWeight="Bold"  x:Name="tzz1"/>
                </StackPanel>
            </StackPanel>
        </Grid>
    </Border>
</Window>
